{
  "feature": "onboarding-flow",
  "branch": "feature/onboarding-flow",
  "prompts": [
    {
      "id": "1",
      "description": "Activity step: hide CTA until selection, reveal with animation",
      "prompt": "In `components/onboarding.tsx`, modify the Activity step (around line 1607) to hide the 'Continue' CTA until the user has selected an activity level, then reveal it with an animation.\n\nFind this block:\n```jsx\n<div className=\"space-y-2.5 pt-0.5\">\n  <OnboardingPrimaryCta\n    className={cn(\n      selectedActivityLevel\n        ? \"bg-primary text-primary-foreground hover:bg-primary/90\"\n        : \"bg-muted text-muted-foreground cursor-not-allowed opacity-50\"\n    )}\n    onClick={moveToNextStep}\n    disabled={!selectedActivityLevel}\n  >\n    Continue with this activity\n  </OnboardingPrimaryCta>\n</div>\n```\n\nReplace it with:\n```jsx\n<div\n  className={cn(\n    \"overflow-hidden transition-all duration-200\",\n    selectedActivityLevel ? \"max-h-[72px] opacity-100\" : \"max-h-0 opacity-0\"\n  )}\n>\n  <div className=\"space-y-2.5 pt-0.5\">\n    <OnboardingPrimaryCta\n      className=\"bg-primary text-primary-foreground hover:bg-primary/90\"\n      onClick={moveToNextStep}\n    >\n      Continue — {activityLevels.find(l => l.id === selectedActivityLevel)?.label} →\n    </OnboardingPrimaryCta>\n  </div>\n</div>\n```\n\nDo NOT change anything else in the activity step. Do NOT change the activityOptionButtonClass function or how activity cards render.\n\nVerify: `npm run build` passes with no TypeScript errors."
    },
    {
      "id": "2",
      "description": "Goal step: hide CTA until selection, reveal with animation and contextual label",
      "prompt": "In `components/onboarding.tsx`, modify the Goal step (around line 1683) to hide the 'Continue' CTA until the user has selected a goal, then reveal it with an animation.\n\nFind this block:\n```jsx\n<div className=\"space-y-2.5 pt-0.5\">\n  <OnboardingPrimaryCta\n    className={cn(\n      selectedGoal\n        ? \"bg-primary text-primary-foreground hover:bg-primary/90\"\n        : \"bg-muted text-muted-foreground cursor-not-allowed opacity-50\"\n    )}\n    onClick={moveToNextStep}\n    disabled={!selectedGoal}\n  >\n    Continue with this goal\n  </OnboardingPrimaryCta>\n</div>\n```\n\nReplace it with:\n```jsx\n<div\n  className={cn(\n    \"overflow-hidden transition-all duration-200\",\n    selectedGoal ? \"max-h-[72px] opacity-100\" : \"max-h-0 opacity-0\"\n  )}\n>\n  <div className=\"space-y-2.5 pt-0.5\">\n    <OnboardingPrimaryCta\n      className=\"bg-primary text-primary-foreground hover:bg-primary/90\"\n      onClick={moveToNextStep}\n    >\n      Continue — {goals.find(g => g.id === selectedGoal)?.label} →\n    </OnboardingPrimaryCta>\n  </div>\n</div>\n```\n\nDo NOT change the goal card rendering or goalOptionButtonClass. Do NOT change any other step.\n\nVerify: `npm run build` passes with no TypeScript errors."
    },
    {
      "id": "3",
      "description": "Diet, ingredient-mode, and quick-estimate body type: hide CTA until selection",
      "prompt": "In `components/onboarding.tsx`, apply the same hide-until-selected CTA reveal pattern to three more steps.\n\n**1. Diet step** (around line 2002): Find:\n```jsx\n<div className=\"space-y-2.5 pt-0.5\">\n  <OnboardingPrimaryCta\n    className={cn(\n      selectedDietType\n        ? \"bg-primary text-primary-foreground hover:bg-primary/90\"\n        : \"bg-muted text-muted-foreground cursor-not-allowed opacity-50\"\n    )}\n    onClick={moveToNextStep}\n    disabled={!selectedDietType}\n  >\n    Continue with this style\n  </OnboardingPrimaryCta>\n</div>\n```\nReplace with a wrapper div using `overflow-hidden transition-all duration-200` and `max-h-[72px] opacity-100` when `!!selectedDietType`, `max-h-0 opacity-0` otherwise. Remove disabled prop. Update CTA label to: `Continue — {dietTypes.find(d => d.id === selectedDietType)?.label} →`. CTA className is always `bg-primary text-primary-foreground hover:bg-primary/90`. Do NOT touch the macroPreview section above the CTA.\n\n**2. Ingredient-mode step** (around line 2103): Find the `<div className=\"space-y-2.5 pt-0.5\">` containing the OnboardingPrimaryCta with `disabled={!ingredientMode}`. Wrap it in an outer div with `overflow-hidden transition-all duration-200` and `max-h-[72px] opacity-100` when `!!ingredientMode`, `max-h-0 opacity-0` when `!ingredientMode`. Remove the disabled prop and the `bg-muted` fallback className. Keep the existing contextual label logic (it already varies by ingredientMode — keep as-is).\n\n**3. Quick-estimate body type** (around line 1510, inside `step === 'quick-estimate' && entryChosen`): Find the `<div className=\"space-y-2.5 pt-0.5\">` containing the OnboardingPrimaryCta 'Set my targets' with `disabled={!weight || !quickHeight || !quickAge || !quickBodyType}`. Wrap it in an outer div with `overflow-hidden transition-all duration-200` and `max-h-[72px] opacity-100` when `!!(weight && quickHeight && quickAge && quickBodyType)`, `max-h-0 opacity-0` otherwise. Remove the disabled prop and muted fallback className. Keep label 'Set my targets'. Keep the paragraph below ('You can refine this later...') and the 'Use detailed body stats' link below OUTSIDE this wrapper — they should always be visible.\n\nDo NOT change card rendering, option button classes, or other steps.\n\nVerify: `npm run build` passes with no TypeScript errors."
    }
  ]
}
